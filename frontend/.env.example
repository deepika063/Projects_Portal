# Frontend environment example for Create React App
# Copy to `frontend/.env` and restart the dev server after editing

# Base URL of the backend API (must start with http:// or https://)
REACT_APP_BASE_URL=http://localhost:5000

# Notes:
# - React environment variables must be prefixed with REACT_APP_ to be visible in the app.
# - Do NOT store secrets (API keys, tokens) in client-side env when committing to public repos.
# - After editing `.env`, restart `npm start` to load new values.
